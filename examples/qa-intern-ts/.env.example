# Solari — one key for browsers + sandboxes: https://console.getsolari.com
SOLARI_API_KEY=slr_live_...

# Claude — either an API key (https://console.anthropic.com) ...
ANTHROPIC_API_KEY=sk-ant-...
# ... or leave it unset after `ant auth login` (the SDK picks up the profile).
